info:
  name: ping
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8008/api/health

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
